// http://judge.telerikacademy.com/problem/11bintodec

const binary = gets().split('').reverse().map(Number);
let number = 0;

for (let i = 0; i < binary.length; i += 1) {
  if (binary[i] === 1) {
    number += Math.pow(2, i);
  }
}

print(number);
